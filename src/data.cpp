#include <iostream>
#include <fstream>
#include <string>
#include "data.h"


MNISTIterator::MNISTIterator(string filename) {
  ifstream inputFile;
  inputFile.open(inputFile);
  inputfile.exceptions(std::ifstream::failbit);
}

